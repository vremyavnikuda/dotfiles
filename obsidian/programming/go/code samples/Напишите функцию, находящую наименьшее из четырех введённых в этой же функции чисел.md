#go #примерыкода #junior #taskGOLANG 
```go
package main  
  
import "fmt"  
  
func searchMinNumber(a, b, c, d int) int {  
    var minNumber int  
    minNumber = min(a, b, c, d)  
    fmt.Println(minNumber)  
    return minNumber  
}  
func main() {  
    var a, b, c, d int  
    fmt.Scanln(&a, &b, &c, &d)  
    searchMinNumber(a, b, c, d)  
}
```